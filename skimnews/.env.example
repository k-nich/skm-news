# Copy to .env for local prod-style testing, or set these in Railway's
# service Variables tab. Never commit a real .env.
DJANGO_SECRET_KEY=replace-with-a-real-key
DJANGO_DEBUG=0
# DATABASE_URL is provided automatically by Railway's Postgres plugin.
# Locally you can leave it unset to use SQLite.
